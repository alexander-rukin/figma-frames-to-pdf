{
  "name": "figma-frames-to-pdf",
  "version": "1.6.0",
  "description": "Export selected Figma frames into a single, lightweight PDF with selectable text.",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "test": "node test/dedupe.test.mjs && node test/compact.test.mjs"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.100.0",
    "esbuild": "^0.23.0",
    "typescript": "^5.5.0"
  },
  "dependencies": {
    "pdf-lib": "^1.17.1"
  }
}
